import React, {useEffect, useState} from 'react';
import {HOST} from '../utils/host';

/**
 * 
 * @param {*} props 
 * @returns a component to remove a food category from the CustomerOrder screen 
 */
export default function RemoveMenuItem(props) {
    const item = props.item;

    function removeOrder(item) {
    }

    return (
        <div>
            
        </div>
    );
}
